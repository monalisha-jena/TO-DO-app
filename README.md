This is a simple To-Do web server using Express.js, without using a database. The
server can manage basic CRUD operations for to-do items with the following
endpoints GET, POST and DELETE using postman.
